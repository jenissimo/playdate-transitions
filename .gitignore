*.pdx/
# tools/build_demo.py stages the root modules and the demo .lua.in sources into
# demo/Source to compile, then removes them. Ignored so an interrupted build
# cannot commit a stale duplicate.
demo/Source/*.lua
demo/Source/images/
.sentry-native/
